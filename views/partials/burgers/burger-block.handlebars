<li>
	{{id}} {{burger_name}}

	<button class="change" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#if devoured}}Devour It!{{/if}}
	</button>
	<button class="delete" data-id="{{this.id}}">DELETE!</button>
</li>

